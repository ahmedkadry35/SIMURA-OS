# SIMURA OS — Calamares packages module config.
# Removes installer-only packages from the freshly-installed system, since
# the user no longer needs Calamares once SIMURA is on disk.

backend: pacman

operations:
  - remove:
      - calamares
      - calamares-tools
      - hwinfo
