# SIMURA OS — unpackfs module.
# Tells Calamares to unpack the squashfs root image (built by archiso) onto
# the user's chosen partition.

unpack:
  - source: "/run/archiso/airootfs"
    sourcefs: "squashfs"
    destination: ""
